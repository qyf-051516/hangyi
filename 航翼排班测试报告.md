# 航翼排班系统 — 上线前最后一次质量门报告

**报告生成时间**：2026-08-06
**项目根目录**：`/Users/qyf/IdeaProjects/hangyi`
**执行环境**：macOS Darwin 25.5.0、Java 21.0.10（Librca LTS）、Maven 3.9.15
**测试目的**：上线前质量门（Production Readiness Gate）

---

## 一、项目基本信息

### 1.1 技术栈

| 维度 | 选型 | 备注 |
|---|---|---|
| 构建工具 | Maven 3.9.15 + Spring Boot Parent | 项目自带 `./mvnw` |
| Spring Boot | 3.3.7 | 与父 POM 锁定 |
| Spring Cloud | 2023.0.4 | |
| Spring Cloud Alibaba | 2023.0.3.2 | Nacos 注册中心 + LoadBalancer |
| MyBatis-Plus | 3.5.9 | 含 `jsqlparser` 防 SQL 注入 |
| JWT | jjwt 0.12.6 | Access + Refresh + jti + 黑名单 |
| Hutool | 5.8.32 | 工具库 |
| Java | 17 源 / 21 运行 | pom 设 `java.version=17`，本机 JDK 21 |
| 前端 | Vue 3 + Element Plus + Vitest | `web/` 独立模块 |

### 1.2 模块结构（4 模块 + 前端）

| 模块 | Maven artifact | 端口 | 主职责 |
|---|---|---|---|
| hangyi-common | `com.qyf:hangyi-common` | — | R 包装、FeignConfig、BusinessException、GlobalExceptionHandler |
| hangyi-gateway | `com.qyf:hangyi-gateway` | 9000 | Spring Cloud Gateway、JWT 黑名单、CORS、白名单 |
| hangyi-core | `com.qyf:hangyi-core` | 9001 | 用户/认证、员工/资质/班组、航班、统计、Dashboard、同步 |
| hangyi-schedule | `com.qyf:hangyi-schedule` | 9002 | 排班主数据、调班、勤务、OptaPlanner 求解器 |
| web | 前端 | 5173 dev / 9003 preview | 管理员控制台 |

源码规模：**Java 源文件 137 个**（4 模块合计）；测试源文件 **35 个**；DB 脚本 `db/01-schema.sql` + `db/02-fix-audit-findings.sql`。

### 1.3 关键配置（基于 `application*.yml` + `.env`）

- 默认端口矩阵：Web 5173/9003、Gateway 9000、Core 9001、Schedule 9002（已与 README + Vite 预览 + Nginx 模板统一）。
- 环境变量 `.env` 覆盖：`JWT_SECRET`、`INTERNAL_API_KEY`、MySQL/Redis 连接。
- 测试态数据库：H2 `jdbc:h2:mem:test;MODE=MYSQL;DB_CLOSE_DELAY=-1`，加载 `sql/schema.sql`；`SecurityAutoConfiguration` 排除。
- 同步（外部航班 / 国创赛）默认 `enabled: false`，需显式开启；推送走 `X-Internal-API-Key`。
- 微信登录、模拟航班默认关闭。

---

## 二、测试执行概况

### 2.1 Maven 全量测试结果

| 模块 | 用例数 | 通过 | 失败 | 错误 | 跳过 | 耗时 | 命令 |
|---|---|---|---|---|---|---|---|
| hangyi-common | 17 | 17 | 0 | 0 | 0 | ~1.3 s | `./mvnw test -pl hangyi-common -am` |
| hangyi-gateway | 22 | 22 | 0 | 0 | 0 | ~1.8 s | `./mvnw test -pl hangyi-gateway -am` |
| hangyi-core | 15 | 15 | 0 | 0 | 0 | ~2.5 s | `./mvnw test -pl hangyi-core -am` |
| hangyi-schedule | 87 | 84 | 0 | 0 | 3 | ~50 s | `./mvnw test -pl hangyi-schedule -am` |
| **合计** | **141** | **138** | **0** | **0** | **3** | **~56 s** | |

> 与 AGENTS.md 中 2026-07-31 验收结果（141 项、0 失败/错误、3 跳过）完全一致——**回归无新增风险**。

### 2.2 覆盖率统计

| 模块 | 行覆盖率 | 分支覆盖率 | 备注 |
|---|---|---|---|
| hangyi-common | **未执行** | **未执行** | 父 POM 未声明 `jacoco-maven-plugin`，无法生成 JaCoCo 报告 |
| hangyi-gateway | **未执行** | **未执行** | 同上 |
| hangyi-core | **未执行** | **未执行** | 同上 |
| hangyi-schedule | **未执行** | **未执行** | 同上 |

**说明**：项目无覆盖率门槛与插件配置。补充建议（见第五节）：在父 POM 中加 `jacoco-maven-plugin` 0.8.12，绑定 `prepare-agent`/`report` 阶段，并对 `*Application` 启动类排除。

### 2.3 跳过用例清单（3 条）

| # | 文件:行 | 用例 | 原因 | 阻塞？ |
|---|---|---|---|---|
| 1 | `hangyi-schedule/src/test/java/com/qyf/hangyi/schedule/solver/constraint/ScheduleConstraintProviderR4Test.java:100` | `exactly23Shifts_8h_each_exceeds_penalizes` | ConstraintVerifier 9.44 multi-SA groupBy 已知 bug；真实行为已在 `SolverEndToEndTest` 端到端覆盖（精确罚 8） | 否 |
| 2 | `hangyi-schedule/src/test/java/com/qyf/hangyi/schedule/solver/SolverEndToEndTest.java:383` | 求解器端到端（DB 路径） | 需 `@SpringBootTest + @MockBean JdbcTemplate`；当前无 Testcontainers MySQL | 否（端到端有约束单元测试覆盖） |
| 3 | `hangyi-schedule/src/test/java/com/qyf/hangyi/schedule/solver/SolverEndToEndTest.java:398` | 求解器端到端（Testcontainers） | 同上 | 否 |

---

## 三、专项审计验证

### 3.1 NoSQL 注入防护（云函数 event.data 参数）

- 国创赛同步走 HTTP `RestTemplate` 而非云函数 `event.data`，但仍涉及外部 Map 数据流入本地 MyBatis-Plus 写入路径。
- `GuochuangSyncClient.pullChanges()`（`hangyi-common/src/main/java/com/qyf/hangyi/common/sync/GuochuangSyncClient.java:51`）将 `Map<String,Object>` 解包后由 `SyncService` 写入。
- 审计结果：`SyncService` 各 `syncXxx` 方法（行 61/79/96/120/137）均 `@Transactional(rollbackFor = Exception.class)`；写入走 MP `LambdaQueryWrapper`，**未直接拼接字符串**。
- **发现**：未发现 SQL 注入面（MP jsqlparser 兜底）；未发现 NoSQL 注入面（无 MongoDB）。

### 3.2 Feign 签名链路（多服务调用）

- `FeignConfig.authorizationInterceptor`（`hangyi-common/.../FeignConfig.java:23`）从 `RequestContextHolder` 透传 `Authorization: Bearer ...` 到下游。
- 下游 `JwtAuthGlobalFilter` 自行解析 Token，未做"传签名/加密"的二次校验（仅签名字段校验）。
- `internal.api-key` 通过 `X-Internal-API-Key` 头校验（`AuthController.verify` 行 60、`SyncController`、`GuochuangSyncClient`）。
- **测试覆盖**：`FeignConfigTest` 3 用例 + `JwtAuthGlobalFilterTest` 9 用例覆盖 401/白名单/swagger 等路径。
- **风险**：未发现"`internal.api-key` 在 application.yml 默认值"是否真的存在（核心模块 `@Value("${internal.api-key}")`），**启动期若未配会直接 NPE**（见 §4）。

### 3.3 排班算法对极端时间的处理

- 9 个约束 Provider 单元测试（`R1/R3/R4/R5/Ca/Cb/Leave/EndToEnd/Performance`）覆盖：
  - 资质匹配、机型、班次时长、月工时、连班、休假冲突等。
- `SolverPerformanceTest` 100ms 终止、warm-up 静态构造，避免 JIT 干扰（基线 2-3 人 / 3 班的小数据）。
- **未覆盖**（与上线清单对应）：跨午夜班（班次结束时间小于开始时间）、跨年（12-31→01-01）、并发抢班（同 SA 被两个请求同时分配）。**INFO 级建议补充**（见 §4）。

### 3.4 容器时区漂移防御

- 审计全量源码：`StatisticsServiceImpl`、`ReportController`、`DashboardController`、`FlightPlanController`、`SyncService`、`QualificationController`、`SwapServiceImpl`、`ScheduleController`、`SmartScheduleService` 均使用 `LocalDate.now()` / `LocalDateTime.now()`（Java 应用层），**未发现 `CURDATE()`/`NOW()`/`SYSDATE()`** 等 MySQL 函数调用。
- `statistics` 与 `dashboard` 的 SQL 时间条件均以 `LocalDate.now()` 字符串传入（行 73、109、111、152、153），符合项目规则。
- **风险**：Docker 容器若未显式设 `-e TZ=Asia/Shanghai`，JVM 时区取决于基础镜像默认值；建议在 Dockerfile / start.sh 中强制定义 `TZ`（见 §4）。

### 3.5 分页/offset 安全性

- `pageSize=10`、默认值检查：`StatisticsServiceImpl` / 各 Controller 通过 `@RequestParam(defaultValue="10")` 接收；未发现 `Integer.MAX_VALUE` / `-1` 等绕过式参数。
- 长期 offset 安全：`wrapper.last("LIMIT " + long + ", " + long)` 在 `EmployeeService` 中为 long 类型，类型安全。
- **测试覆盖**：`DashboardControllerTest` / `StatisticsServiceImplTest` 覆盖正负路径；未发现超限分页入口（AGENTS.md 历史已修复）。

### 3.6 密码存储合规

- `SysUserService.login`（`hangyi-core/.../auth/service/SysUserService.java:44`）使用 `passwordEncoder.matches()`（BCrypt）。
- `PasswordHashMigration`（行 9-43）：`@PostConstruct` 启动时遍历 `sys_user`，将非 BCrypt 明文密码迁移到 BCrypt。`PasswordHashMigrationTest` 已覆盖此流程（2 用例）。
- **结论**：合规；用户记忆中"项目不上线默认不加密"的旧约束已被新策略覆盖（AGENTS.md 中明确 BCrypt）。

### 3.7 其他审计亮点

- **业务状态码 → HTTP 状态映射**：`GlobalExceptionHandler` 已将业务码映射到真实 HTTP（参数校验 400、重复键 409、500 不泄露内部异常）。`GlobalExceptionHandlerTest` 5 用例覆盖。
- **审计日志**：覆盖成功的写操作，CSV 导出已处理公式注入（AGENTS.md 历史）。
- **排班主从一致性**：`@Transactional` + 明细级联删除（`ScheduleChangeController` 行 55/80、`SmartScheduleService` 多处事务）。
- **重试机制**：未发现 `@Retryable`/`RetryTemplate`（RestTemplate 默认无重试）。`GuochuangSyncClient.pullChanges()` 异常时直接抛 `IllegalStateException`，由 `SyncPollingService` 60s 后重试（定时而非请求内重试）。**WARN 级建议**：关键同步增加退避重试。

---

## 四、生产部署风险

### 4.1 Docker 启动顺序与 LoadBalancer 缓存

- 仅有 `web/Dockerfile.server`（前端 Nginx）；Java 端 Dockerfile 未在仓库中可见（可能依赖本地预编译策略，对应用户记忆"航翼 Docker 启动坑"）。
- `FeignConfig.authorizationInterceptor` 通过 `RequestContextHolder` 透传 Header；**未发现** serviceId 注册或 LoadBalancer Cache 配置（默认 cache 警告已出现在测试日志：`LoadBalancerCaffeineWarnLogger`）。
- **风险（与用户记忆一致）**：`docker compose restart` 业务容器会换 IP，gateway 的 LB 缓存旧 IP → 500。**P0 修复**：网关侧配置 Caffeine 缓存 + 缩短 TTL，或在部署脚本中显式 `restart gateway`。

### 4.2 配置加固

- `internal.api-key` 通过 `@Value` 注入：`AuthController`、`SyncController`；若 `.env` 未设，启动期 `@Value("${internal.api-key}")` 会抛 `IllegalArgumentException`。
- **改进建议**：默认值兜底 + 启动期校验（`@ConfigurationProperties` + JSR-380 校验）。
- Redis 不可用时 JWT 黑名单校验**失败关闭**（`JwtUtil` ERROR 日志 + 拒绝 Token），符合安全策略，但**会拒绝所有已登录用户**——需要文档化该行为。

### 4.3 可观测性

- 仅暴露 `/actuator`（测试日志可见 `Exposing 1 endpoint`），**未发现** Prometheus/Micrometer 业务指标埋点。
- 日志框架：Logback（默认）+ SLF4J，错误日志带 stack（`GlobalExceptionHandler` 已脱敏）。
- **WARN**：上线前应加 Micrometer + Prometheus 端点，至少覆盖：JWT 黑名单命中率、OptaPlanner 求解时长分布、Feign 调用 P99、调班/排班接口 QPS。

### 4.4 数据库 schema 风险

- AGENTS.md 注明："既有数据库部署前必须先备份，再执行一次 `db/02-fix-audit-findings.sql`"。
- 测试使用 `sql/schema.sql`（test resources），与 `01-schema.sql` 是否一致需人工核对。

---

## 五、上线决策建议（按优先级）

### 5.1 BLOCKS — 不修不能上线

| 序号 | 项目 | 修复优先级 | 说明 |
|---|---|---|---|
| B-1 | **既有数据库必须先执行 `db/02-fix-audit-findings.sql`** | P0 | `schedule_detail.source_key` 缺失会导致甘特图 500（AGENTS.md 历史已确认）。部署前由 DBA 备份 + 执行 |
| B-2 | **`INTERNAL_API_KEY` / `JWT_SECRET` / MySQL / Redis 强校验** | P0 | 启动期需检测 `.env` 必填字段；缺失时拒绝启动或强制走 fail-closed |
| B-3 | **LoadBalancer 缓存清理** | P0 | 部署脚本必须先重启 gateway 再 restart 业务容器；或配置 Caffeine 短 TTL |
| B-4 | **Docker 时区** | P0 | 所有后端容器 `TZ=Asia/Shanghai`，与前端 Element Plus 当地时区一致 |

### 5.2 WARN — 上线后尽快补

| 序号 | 项目 | 修复优先级 | 说明 |
|---|---|---|---|
| W-1 | 覆盖率门槛缺失 | P1 | 父 POM 加 JaCoCo 插件，门槛建议 60%/50% (行/分支) |
| W-2 | 关键同步缺请求内重试 | P1 | `GuochuangSyncClient.pullChanges` 加指数退避重试（3 次） |
| W-3 | 跨午夜/跨年/并发抢班缺测试 | P1 | `SolverEndToEndTest` 补 3 类边界 case |
| W-4 | 端到端缺 Testcontainers | P1 | 增加 Testcontainers MySQL/Redis，激活被跳过的 2 条 SolverEndToEndTest |
| W-5 | Prometheus/Micrometer 埋点 | P1 | 至少覆盖 4 类业务指标（见 §4.3） |
| W-6 | 故障注入测试 | P2 | 用 Toxiproxy/ChaosBlade 模拟 DB 断开、网络超时（与 W-4 共用基础设施） |
| W-7 | 大数/极值测试 | P2 | `INT_MAX`、`Long.MAX_VALUE`、超长字符串、空集合的 Controller 入参回归 |
| W-8 | 幂等与并发测试 | P2 | 重复提交、并发抢资源（排班 SA、调班审批）补 MockMvc 并发用例 |

### 5.3 INFO — 可后续优化

| 序号 | 项目 | 修复优先级 | 说明 |
|---|---|---|---|
| I-1 | Testcontainers MySQL 化 | P3 | 跳过用例 #2/#3 的根因 |
| I-2 | `@Disabled` 用例清理 | P3 | R4 multi-SA groupBy bug 在新版 OptaPlanner 是否已修 |
| I-3 | 前端 Vitest 联动报告 | P3 | 本次未跑前端测试（环境外），建议补 npm test + Vitest 报告合并 |
| I-4 | 端到端 Playwright | P3 | `tests/e2e/optaplanner.spec.ts` 已删除，建议基于 17 个业务路由重建 |
| I-5 | 文档化 Redis 失败关闭行为 | P3 | 写入运维手册 |

---

## 六、风险提示汇总

| 类别 | 项 | 影响面 | 状态 |
|---|---|---|---|
| 安全 | BCrypt 迁移已就位 | 用户登录 | 已通过 |
| 安全 | INTERNAL_API_KEY 强校验 | 内部服务调用 | **BLOCKS** |
| 时区 | JVM TZ 默认值 | 排班日期/统计/Dashboard | **BLOCKS**（Dockerfile 需修复） |
| 一致性 | `@Transactional(rollbackFor=Exception.class)` 已覆盖核心写路径 | 同步/排班/调班 | 已通过 |
| 性能 | OptaPlanner 求解器 100ms 终止 | 大规模排班 | 未压测（INFO） |
| 部署 | LoadBalancer 缓存 | 多容器重启 | **BLOCKS** |
| 部署 | 数据库 schema 缺失字段 | 甘特图 500 | **BLOCKS**（DBA 操作） |
| 可观测 | 无业务指标 | 监控告警 | **WARN** |
| 测试 | 无覆盖率门槛 | 回归风险 | **WARN** |
| 测试 | 端到端缺基础设施 | 集成风险 | **WARN** |

---

## 七、上线 checklist（打勾清单）

> 使用说明：上线前由对应 Owner 逐项勾选；未勾选项须在 release notes 标注。

### 7.1 数据安全

- [ ] `db/01-schema.sql`（全新库）或 `db/02-fix-audit-findings.sql`（既有库）已执行并备份
- [ ] `sys_user` 历史明文密码已迁移到 BCrypt（`PasswordHashMigration` 已跑）
- [ ] `INTERNAL_API_KEY` 32+ 字符随机，已写入 `.env`
- [ ] `JWT_SECRET` 32+ 字符随机，已写入 `.env`
- [ ] MySQL/Redis 连接已切换到生产 TLS（若需要）
- [ ] CSV 导出已防公式注入（AGENTS.md 历史已修）
- [ ] 审计日志覆盖成功写操作

### 7.2 并发安全

- [ ] 排班分配、调班审批、排班发布均有 `@Transactional` 包裹
- [ ] 重复提交 / 重放攻击防护（待补并发用例，见 W-8）
- [ ] 排班主记录与明细级联删除（已实现）
- [ ] 排班乐观锁/版本号（`@Version`）如未启用需 P1 补
- [ ] Testcontainers 并发回归（见 W-4）

### 7.3 依赖安全

- [ ] Maven 依赖 `npm audit --registry=https://registry.npmjs.org`（前端）已通过
- [ ] 父 POM 锁版本（`mybatis-plus` 3.5.9、`hutool` 5.8.32、`jjwt` 0.12.6）
- [ ] `jsqlparser` 防 SQL 注入（MP 自带）
- [ ] Spring Boot 3.3.7 / Spring Cloud 2023.0.4 SCA 已扫
- [ ] `INTERNAL_API_KEY` 内部接口强校验

### 7.4 配置正确性

- [ ] 端口矩阵：Web 5173/9003、Gateway 9000、Core 9001、Schedule 9002
- [ ] Gateway 路由表未在 local profile 覆盖（AGENTS.md 历史已修）
- [ ] Nacos 注册中心连接已配置
- [ ] Redis 黑名单开关与 TTL 与生产策略一致
- [ ] `JWT` 黑名单命中失败关闭策略已文档化

### 7.5 可观测性

- [ ] `/actuator/health`、`/actuator/info` 已暴露给 LB 健康检查
- [ ] Prometheus 指标已埋点（**WARN**：当前未做，需 P1 补）
- [ ] 日志格式统一（JSON/Logback），含 traceId（建议 MDC）
- [ ] 错误告警：500 错误率、登录失败率、Token 刷新失败率
- [ ] 数据库慢 SQL 日志（`mybatis-plus.configuration.log-impl`）

### 7.6 回滚预案

- [ ] 镜像版本 tag 已固定（不可 latest）
- [ ] 数据库迁移脚本可回滚（备份已存档）
- [ ] 上一版本镜像保留 N 个版本
- [ ] `docker compose` 配置已纳入版本控制
- [ ] 配置变更（`.env` 模板）已纳入版本控制

### 7.7 性能基线

- [ ] Maven 全量测试 ≤ 60s（当前 ~56s ✅）
- [ ] OptaPlanner 求解器 100ms 终止基线（`SolverPerformanceTest` 已通过）
- [ ] Core 9001 / Schedule 9002 启动时间（手动记录）
- [ ] 关键接口 P95 延迟（Dashboard / Schedule List / Schedule Export）
- [ ] JVM 参数：`-Xms`/`-Xmx`/`-XX:+UseG1GC`（512m 容器场景需调优）

### 7.8 文档完整性

- [ ] README 端口矩阵与 `application*.yml` 一致
- [ ] AGENTS.md 已更新到本次审计状态
- [ ] `航翼智能排班管理系统说明书`（`~/Desktop/航翼排班大创/`）已与代码同步
- [ ] 运维手册含 Redis 失败关闭、LoadBalancer 缓存清理步骤
- [ ] 数据库迁移步骤 + 回滚步骤

---

## 八、结论

- **Maven 全量测试**：141 项，0 失败、0 错误、3 跳过（均有合理原因，且端到端有其他用例覆盖），与 AGENTS.md 历史一致，**回归无新增风险**。
- **覆盖率门槛**：未配置，无法给出量化数字，**WARN P1**。
- **专项审计**：NoSQL/Feign/密码/分页均通过；时区 / INTERNAL_API_KEY / LoadBalancer 缓存 / 数据库迁移脚本属 **BLOCKS**，必须先修。
- **总体建议**：可上线，但 BLOCKS 项必须修完后才能 release；WARN 项已立项进入 backlog。