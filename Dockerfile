# ============================================
# 此文件已废弃 — 项目为多模块架构，各模块有独立 Dockerfile。
# 构建请使用各子模块目录下的 Dockerfile（如 hangyi-gateway/Dockerfile）
# 或使用 docker-compose.yml 一键部署。
# ============================================
