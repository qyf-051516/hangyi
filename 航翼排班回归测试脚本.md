# 航翼排班 · 上线前回归测试脚本

> 与 `~/Desktop/航翼排班修复TODO.md` 配合使用。每套脚本验证对应 BLOCKS 修复项，**全部绿**才能上线。
> 项目根：`~/IdeaProjects/hangyi`，部署根：`~/hangyi-deploy`

## 套件总览

| 编号 | 验证 BLOCKS | 预计耗时 |
|---|---|---|
| REG-H1 | H1 DB 字段迁移 | 5 分钟 |
| REG-H2 | H2 时区固化 | 5 分钟 |
| REG-H3 | H3 LoadBalancer 缓存 | 10 分钟 |

---

## REG-H1 · DB 字段迁移验证

**前置**：BLOCKS-H1 已执行

```bash
# 1. 字段存在性
mysql -u root -p hangyi -e "SHOW COLUMNS FROM schedule_detail LIKE 'source_key'"
# 期望: source_key varchar(64) ... 且 key 列含 UNI

# 2. 唯一索引存在
mysql -u root -p hangyi -e "SHOW INDEX FROM schedule_detail WHERE Key_name='uk_source_key'"

# 3. NULL 数据检查
mysql -u root -p hangyi -e "SELECT COUNT(*) FROM schedule_detail WHERE source_key IS NULL"
# 如果有 NULL 数据 → 跑 UPDATE 补字段再回归

# 4. SyncServiceTest
cd ~/IdeaProjects/hangyi
./mvnw -pl hangyi-core test -Dtest=SyncServiceTest -q
```

**通过条件**：
- [ ] 字段存在、类型正确、唯一索引存在
- [ ] SyncServiceTest 全绿
- [ ] 没有 NULL 数据遗留

---

## REG-H2 · Docker 时区固化验证

**前置**：BLOCKS-H2 已修改 `docker-compose.override.yml` 或 Dockerfile

```bash
# 1. 重启服务让环境变量生效
cd ~/hangyi-deploy
docker compose -f docker-compose.yml -f docker-compose.override.yml up -d

# 2. 验证容器时区
for svc in core schedule gateway; do
  echo "=== $svc ==="
  docker exec hangyi-$svc sh -c 'echo $TZ && date && date -u'
done
# 期望三个服务都输出 Asia/Shanghai

# 3. 接口验证日期字段本地化
curl -s http://localhost:9001/api/health
curl -s http://localhost:9001/api/schedule/today | jq '.date'
# 期望是 Asia/Shanghai 日期字符串，不是 UTC

# 4. 跨日边界测试
echo "Now: $(date)"
curl -s "http://localhost:9001/api/schedule/gantt?date=$(date +%Y-%m-%d)" | jq '.date'
# 23:59:59 调用一次, 00:00:01 调用一次, 验证日期字段不漂移
```

**通过条件**：
- [ ] 三个服务 `TZ=Asia/Shanghai` 且 `date` 输出 CST
- [ ] 接口返回日期字段本地化（不是 UTC）
- [ ] 跨日边界调用日期不漂移

---

## REG-H3 · LoadBalancer 缓存验证

**前置**：BLOCKS-H3 已修（重启脚本顺序 或 Caffeine TTL=5s）

```bash
# 验证基础调用
for i in 1 2 3; do
  echo "=== Try $i ==="
  curl -s -w "\nHTTP %{http_code}, time=%{time_total}s\n" \
    http://localhost:9000/schedule/api/v1/health -o /dev/null
done

# 重启 schedule 服务（关键步骤）
cd ~/hangyi-deploy
docker compose restart schedule
sleep 2

# 立即调用：必须不 500
for i in 1 2 3; do
  echo "=== After restart, try $i ==="
  curl -s -w "\nHTTP %{http_code}, time=%{time_total}s\n" \
    http://localhost:9000/schedule/api/v1/health -o /dev/null
done

# 如果是 Caffeine TTL 方案：检查日志
docker logs hangyi-gateway 2>&1 | grep -i "loadbalanc\|caffeine\|cache ttl"
```

**通过条件**：
- [ ] restart schedule 后调用 gateway 不返回 500（要么立即成功，要么 ≤5s 后成功）
- [ ] 日志显示 LoadBalancer 缓存正常失效

---

## 上线最终判定

```bash
# 跑一遍快速 smoke
curl -s http://localhost:9001/api/health  # 航翼 core
curl -s http://localhost:9002/api/health  # 航翼 schedule
curl -s http://localhost:9000/api/health  # 航翼 gateway
# 必须都 200
```

**上线 checklist：**
- [ ] 3 套回归脚本全绿
- [ ] 监控/告警到位（gateway 5xx 告警）
- [ ] 回滚脚本 ready（一键回滚到上一个版本）
- [ ] 数据库迁移可回滚（每个 ALTER TABLE 都有对应 DOWN）
- [ ] 配置文件变更记录到 CHANGELOG
- [ ] 值班同学手机开机

**所有脚本绿 + 所有 checklist 勾选 = 航翼排班可以上线**